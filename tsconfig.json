{
	"include": ["src/**/*"],
	"compilerOptions": {
    // 使用而是017编译
    "target": "es2017",
    //解析非相对模块名的基准目录。查看 模块解析文档了解详情。
    "baseUrl": ".",
    //路径映射的列表
    "paths": {
      "@/*": [
        "src/*"
      ]
    },
    // 仅用来控制输出的目录结构 --outDir
    "jsx": "react",
    "rootDir": "src",
    "outDir": "build",
    //添加全局变量
    "types": [
      "node"
    ],
    //全局变量路径
    "typeRoots": [
      "node_modules/@types"
    ],
    // 以严格模式解析
    "strict": true,
    // 启用装饰器
    "experimentalDecorators": true,
    // 采用的模块系统
    "module": "commonjs",
    // 如何处理模块
    "moduleResolution": "node",
    // 在表达式和声明上有隐含的any类型时报错
    "noImplicitAny": true,
    //生成相应的 .map文件。
    "sourceMap": true,
    // 给错误和消息设置样式，使用颜色和上下文。
    "pretty": true,
    //编译 js 的时候，删除掉注释
    "removeComments": true,
    // 库的列表
    "lib":[
      "es2015",
      "es2016",
      "es2017",
      "dom"
    ]
	}
}
